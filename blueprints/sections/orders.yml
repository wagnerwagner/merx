headline: field.orders
type: pages
parent: site.ordersPage
template: order
layout: table
create: false
search: true
image: false
columns:
  email:
    value: "{{ page.email }}"
  created:
    value: "{{ page.created.toDate('c') }}"
  total:
    value: "{{ page.total }}"
  payment:
    value: "{{ page.paymentMethod }}"
