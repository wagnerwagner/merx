label: field.prices
type: structure
translate: false
columns:
  price:
    mobile: true
  pricingKey:
    mobile: true
fields:
  price:
    extends: fields/price
    before: null
    required: false
    width: 1/2
  pricingKey:
    label: field.pricing
    type: select
    options:
      type: query
      query: site.pricingRules
      text: "{{ item.name }}"
      value: "{{ item.key }}"
    width: 1/2
    required: true
